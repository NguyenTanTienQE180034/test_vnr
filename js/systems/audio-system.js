(function () {
  const App = (window.App = window.App || {});

  // Placeholder audio layer: ready for future SFX assets.
  App.audioSystem = {
    enabled: false,
    init() {},
    play() {},
  };
})();
